NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_APP_NAME=
# Tenant ID this deployment is bound to. Each per-tenant branch sets its own value.
# Backend uses this (via X-Tenant-Id header) to resolve tenant on public endpoints
# like lead capture and self-register. Empty in the template/main branch.
NEXT_PUBLIC_TENANT_ID=
